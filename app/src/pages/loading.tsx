import React from 'react'

const Loadung = () => {
  return (
    <div>
      Wait, till the summary is received.
      Once received, the summary will automatically display.
      Thank you for your patience.
    </div>
  )
}

export default Loadung
